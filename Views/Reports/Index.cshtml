@model WorkshopManager.Models.ReportFilterViewModel

<form asp-action="Generate" method="post">
    <div class="form-group">
        <label for="CustomerEmail">Klient</label>
        <select asp-for="CustomerEmail" class="form-control" asp-items="Model.Customers">
            <option value="">-- wybierz klienta --</option>
        </select>
    </div>
    
    <div class="form-group">
        <label for="LicensePlate">Tablica rejestracyjna</label>
        <select asp-for="LicensePlate" class="form-control" asp-items="Model.LicensePlates">
            <option value="">-- wybierz pojazd --</option>
        </select>
    </div>

    <div class="form-group">
        <label for="Month">Miesiąc</label>
        <input asp-for="Month" class="form-control" placeholder="rrrr-mm" />
    </div>

    <button type="submit" class="btn btn-primary">Generuj raport</button>
</form>